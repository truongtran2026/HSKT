import PagePlaceholder from "@/components/PagePlaceholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Cài đặt chung"
      stage="giai đoạn 2 (multi-user) — chưa cần ở MVP"
      desc="Đăng nhập, phân quyền Admin/Edit/Viewer theo bảng, nhật ký hoạt động — thiết kế component sẵn sàng để thêm sau, chưa bật ở MVP single-user."
    />
  );
}
